- LogService.cs has harcoded log path

- Check whether file exist before reading log

- Implement DTO validatio n despite being handled in custom validation (e.g., [Required], [MaxLength])

- Introduce CancellationToken in Application

- Donot change editorconfig (for now)